# Dev / CI only — not installed into the production Docker image.
# Install: pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
respx>=0.21
ruff>=0.8
mypy>=1.13
