# Broad-except allowlist for backend/app.
# Only justified rollback/re-raise or similar must appear here.
#
# Preferred format (stable across line shifts):
#   path/to/file.py:except Exception  # reason
#   path/to/file.py:except:           # reason (bare)
#   path/to/file.py:except BaseException  # reason
#
# Also accepted: path/to/file.py:LINE  # reason (pins a single line)
#
# Kind match is EXACT: `except Exception` never covers bare `except:`.
# Keep this list tiny. Prefer narrowing the except when possible.

backend/app/services/email_reveal.py:except Exception  # rollback-and-reraise after BEGIN IMMEDIATE so terminal email cache is not left half-applied; error is never swallowed
backend/app/services/observability.py:except BaseException  # re-record trace on any outbound failure then re-raise; never swallow
backend/app/services/job_sources/registry.py:except Exception  # isolate source failures so one adapter never aborts multi-source search; error counted and surfaced
backend/app/services/job_sources/sources.py:except Exception  # isolate per-slug ATS board failures; sibling slugs continue; error logged
